services:
  homelabweb:
    image: nginx:latest
    hostname: homelabweb
    container_name: homelabweb
    working_dir: /var/www
    # extra_hosts:
    #   - "homelab.localhost=127.0.0.1"
    #   - "jobcon.localhost=127.0.0.1"
    #   - "projeto1.localhost=127.0.0.1"
    #   - "projeto2.localhost=127.0.0.1"
    ports:
      - ${HOMELAB_WEB_NGINX_PORT}:80
      - ${HOMELAB_WEB_NGINX_SECURE_PORT}:443
    volumes:
      - ./config/nginx/sites-available:/etc/nginx/sites-available
      - ./config/nginx/sites-enabled:/etc/nginx/sites-enabled
      - ./config/nginx/nginx.conf:/etc/nginx/nginx.conf
    restart: unless-stopped
    networks:
      lan:
  homelabrds:
    image: mysql:latest
    hostname: homelabrds
    container_name: homelabrds
    extra_hosts:
      - "rds.homelab=127.0.0.1"
    ports:
      - ${HOMELAB_RDS_MYSQL_PORT}:3306
    volumes:
      - ./docker/volumes/mysqldata:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=${HOMELAB_RDS_MYSQL_ROOT_PASSWORD}
      - MYSQL_DATABASE=${HOMELAB_RDS_MYSQL_DATABASE}
      - MYSQL_USER=${HOMELAB_RDS_MYSQL_USER}
      - MYSQL_PASSWORD=${HOMELAB_RDS_MYSQL_PASSWORD}
    restart: unless-stopped
    networks:
      lan:
  # portainer:
  #   image: portainer/portainer-ce:lts
  #   container_name: portainer
  #   hostname: portainer
  #   ports:
  #     - ${PORTAINER_PORT}:9000
  #     - ${PORTAINER_SECURE_PORT}:9443
  #   volumes:
  #     - /var/run/docker.sock:/var/run/docker.sock
  #     - ./config/volumes/portainerdata:/data
  #   restart: unless-stopped
  #   networks:
  #     homelab:
  # grafana:
  #   image: grafana/grafana
  #   container_name: grafana
  #   restart: unless-stopped
  #   environment:
  #     - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN}
  #     - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD} #change the password
  #     - GF_INSTALL_PLUGINS=
  #   ports:
  #     - ${GRAFANA_PORT}:3000
  #   volumes:
  #     - grafana_data:/var/lib/grafana
  #   networks:
  #     - homelab  
  # keycloak:
  #   image: quay.io/keycloak/keycloak:24.0
  #   environment:
  #     KC_HOSTNAME: localhost
  #     KC_HOSTNAME_PORT: 7080
  #     KC_HOSTNAME_STRICT_BACKCHANNEL: "true"
  #     KEYCLOAK_ADMIN: admin
  #     KEYCLOAK_ADMIN_PASSWORD: admin
  #     KC_HEALTH_ENABLED: "true"
  #     KC_LOG_LEVEL: info
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:7080/health/ready"]
  #     interval: 15s
  #     timeout: 2s
  #     retries: 15
  #   command: ["start-dev", "--http-port", "7080", "--https-port", "7443"]
  #   ports:
  #     - "7080:7080"
  #     - "7443:7443"
  #   networks:
  #     - homelab  
networks:
  lan:
    driver: bridge
