server {
    listen 80;
    server_name localhost painel.home;
    index index.php index.html index.htm;

    location / {
        proxy_pass http://painel_nginx:80;
        proxy_set_header Host $host;
    }
}