services:
  nginx:
    image: nginx:alpine
    container_name: proxy
    ports:
      - ${WEB_SERVER_PORT}:80
      - ${WEB_SERVER_SECURE_PORT}:443
    volumes:
      - ./vhosts/default.conf:/etc/nginx/conf.d/default.conf
      - ./vhosts/enabled:/etc/nginx/conf.d
    networks:
      - proxy
    restart: unless-stopped
networks:
  proxy:
    name: proxy
    driver: bridge